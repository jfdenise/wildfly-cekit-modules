<?xml version="1.0" ?>

<config xmlns="urn:jboss:galleon:config:1.0" model="standalone">
    <props>
        <prop name="--server-config" value="standalone.xml"/>
    </props> 
    <packages>
        <!-- We need to split this one to have the logging.properties be not excludable -->
        <package name="wildfly.s2i.common" optional="true"/>
        <package name="org.wildfly.cloud.bootable.runtime" optional="true"/>
    </packages>
    <layers>
        <!-- required for termination and script execution -->
        <include name="core-tools"/>
        <include name="health"/>
    </layers>
</config>